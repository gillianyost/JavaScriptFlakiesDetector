# Pull base node:lts-fermium
FROM node:lts-fermium

RUN
apt-get update && apt-get -y install stress-ng
useradd -ms /bin/bash -c "flakie" flakie && echo "flakie:docker"
npm install -g jest @jest/test-sequencer
export NODE_PATH=$(npm root --quiet -g)
export CI=true
