How to run flakyDetector.py:
1. sudo docker run --rm -it -v $(pwd):/home/project node:lts-fermium /bin/bash
2. cd /home/project
3. npm install seedrandom
4. npm install -g jest @jest/test-sequencer
5. export NODE_PATH=$(npm root --quiet -g)
6. export CI=true
7. ./iterateNpmModules.sh multi-suite-npm-modules.txt


How to compile RandomSequencer (must be compiled if changed before running flakyDetector):
Follow steps from trial 1.
